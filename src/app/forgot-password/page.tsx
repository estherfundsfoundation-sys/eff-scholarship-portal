import {requestPasswordReset} from "@/app/auth/actions";
export default async function Forgot({searchParams}:{searchParams:Promise<{message?:string;error?:string}>}){const {message,error}=await searchParams;return <main className="section"><div className="shell" style={{maxWidth:520}}><div className="card"><div className="eyebrow">Account recovery</div><h2>Reset password</h2><p className="muted">Enter your account email. For privacy, we show the same confirmation whether or not it exists.</p>{error&&<p className="notice" role="alert">{error}</p>}{message&&<p className="notice" role="status">{message}</p>}<form action={requestPasswordReset} style={{display:"grid",gap:16}}><label>Email address<input name="email" type="email" required autoComplete="email" style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><button className="button">Send reset link</button></form></div></div></main>}
