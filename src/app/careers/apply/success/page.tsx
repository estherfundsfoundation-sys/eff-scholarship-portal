import Link from "next/link";
import {CheckCircle2,Mail,ShieldCheck} from "lucide-react";
import {careerRoleMap} from "@/lib/careers";

export default async function CareerApplicationSuccess({searchParams}:{searchParams:Promise<{role?:string;id?:string}>}){const {role:slug,id}=await searchParams;const role=slug?careerRoleMap.get(slug):null;return <main className="section white"><div className="shell career-success"><CheckCircle2/><div className="eyebrow">Application received</div><h2>Thank you for stepping forward.</h2><p>We received your application{role?` for ${role.title}`:""}. If your experience and availability align with the next stage, the EFF National Office will contact you from an official <strong>estherfundsinc.org</strong> email address.</p><div className="career-success-note"><ShieldCheck/><p>Submitting an application does not create an employment, contractor, volunteer, fiduciary, or attorney-client relationship. Do not email Social Security numbers, tax records, bank information, passwords, or verification codes.</p></div><div className="hero-actions"><Link className="button" href={id?`/careers/applications/${id}`:"/careers/dashboard"}>View careers dashboard</Link><a className="button outline" href="mailto:nationals@estherfundsinc.org"><Mail size={17}/> Contact the National Office</a></div></div></main>}
