import {notFound} from "next/navigation";import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {applicantLabels,type ApplicationStatus} from "@/lib/domain";
export default async function ApplicationDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data:a}=await supabase.from("applications").select("id,status,submitted_at,created_at,updated_at,program_cycles(name,programs(name)),status_history(previous_status,new_status,applicant_note,created_at)").eq("id",id).single();if(!a)notFound();const history=(a.status_history as unknown as Array<{new_status:ApplicationStatus;applicant_note:string|null;created_at:string}>)??[];return <main className="section"><div className="shell" style={{maxWidth:800}}><Link className="card-link" href="/dashboard">← Dashboard</Link><div className="card" style={{marginTop:18}}><div className="eyebrow">{applicantLabels[a.status as ApplicationStatus]}</div><h2>{(a.program_cycles as unknown as {programs:{name:string}})?.programs?.name??"EFF application"}</h2><p className="muted">Application ID: {a.id}</p><h3>Status timeline</h3>{history.length?<ol>{history.sort((x,y)=>x.created_at.localeCompare(y.created_at)).map((h,i)=><li key={`${h.created_at}-${i}`}><strong>{applicantLabels[h.new_status]??h.new_status}</strong> — {new Date(h.created_at).toLocaleString()}{h.applicant_note&&<p>{h.applicant_note}</p>}</li>)}</ol>:<p className="muted">Your draft has been created. Status updates will appear here.</p>}</div></div></main>}
