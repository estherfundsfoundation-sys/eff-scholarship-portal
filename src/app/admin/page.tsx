const metrics=[["Applied","0"],["In review","0"],["Action needed","0"],["Decisions pending","0"]];
export default function Admin(){return <main className="section white"><div className="shell"><div className="eyebrow">Administration</div><h2>Command center</h2><p className="muted">Permission-scoped operational view. All counts are queried from live records when Supabase is configured.</p><div className="stats" style={{margin:"2rem 0"}}>{metrics.map(([x,n])=><div className="stat" key={x}><strong>{n}</strong><span>{x}</span></div>)}</div><div className="cards"><div className="card"><h3>Applications</h3><p>Review submissions, assign reviewers, request information, and record decisions.</p></div><div className="card"><h3>Name Your Need import</h3><p>Map, validate, dry-run, reconcile, and invite legacy applicants securely.</p></div><div className="card"><h3>Trusted sources</h3><p>Monitor adapters, exceptions, canonical records, and link health.</p></div></div></div></main>}
