import Link from "next/link";import {requireAdmin} from "@/lib/auth/staff";import {updateCycle,updateFormVersion} from "./actions";
export default async function ProgramsAdmin(){const {supabase}=await requireAdmin();const {data:programs}=await supabase.from("programs").select("id,slug,name,description,active,program_cycles(id,name,opens_at,closes_at,status,award_config,form_versions(id,version,schema,published_at),policy_versions(id,version,published_at))").order("name");return <main className="section white"><div className="shell"><Link className="card-link" href="/admin">← Command center</Link><div className="eyebrow">Versioned program configuration</div><h2>Programs, cycles, and forms</h2><div className="notice">Changing an open cycle can affect applicants. Historical applications remain tied to their original form and policy versions.</div>{programs?.map(program=><section className="card" key={program.id} style={{marginTop:24}}><div className="eyebrow">{program.active?"Active":"Inactive"}</div><h3>{program.name}</h3><p>{program.description}</p>{(program.program_cycles as unknown as Array<{id:string;name:string;opens_at:string|null;closes_at:string|null;status:string;award_config:unknown;form_versions:Array<{id:string;version:number;schema:unknown;published_at:string|null}>}>).map(cycle=><div className="admin-columns" key={cycle.id}><form action={updateCycle} className="stack"><h3>Cycle {cycle.name}</h3><input type="hidden" name="cycle_id" value={cycle.id}/><label>Name<input name="name" defaultValue={cycle.name} required/></label><label>Opens<input name="opens_at" type="datetime-local" defaultValue={cycle.opens_at?.slice(0,16)}/></label><label>Closes<input name="closes_at" type="datetime-local" defaultValue={cycle.closes_at?.slice(0,16)}/></label><label>Status<select name="status" defaultValue={cycle.status}><option value="draft">Draft</option><option value="open">Open</option><option value="closed">Closed</option><option value="archived">Archived</option></select></label><label>Award configuration JSON<textarea name="award_config" defaultValue={JSON.stringify(cycle.award_config,null,2)} rows={6}/></label><button className="button">Save cycle</button></form><div>{cycle.form_versions.map(form=><form action={updateFormVersion} className="stack" key={form.id}><h3>Form version {form.version}</h3><input type="hidden" name="form_version_id" value={form.id}/><label>Schema JSON<textarea name="schema" defaultValue={JSON.stringify(form.schema,null,2)} rows={14}/></label><label><input type="checkbox" name="published" defaultChecked={Boolean(form.published_at)}/> Published</label><button className="button outline">Save form version</button></form>)}</div></div>)}</section>)}</div></main>}
