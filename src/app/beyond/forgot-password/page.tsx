import {requestPasswordReset} from "@/app/auth/actions";

export default async function BeyondForgotPassword({searchParams}:{searchParams:Promise<{message?:string;error?:string}>}){const {message,error}=await searchParams;return <main className="beyond-form-page"><section><p className="beyond-kicker">Account recovery</p><h1>Find your way <em>back.</em></h1><p>We will send a private, one-time link to the email connected to your Beyond account.</p></section><form action={requestPasswordReset} className="beyond-form"><p className="beyond-kicker dark">Reset your password</p><h2>Secure your account.</h2>{error&&<p className="beyond-alert" role="alert">{error}</p>}{message&&<p className="beyond-alert success" role="status">{message}</p>}<label>Email address<input name="email" type="email" required autoComplete="email"/></label><button className="beyond-button">Send my reset link</button><p>For privacy, the same confirmation appears whether or not an account exists.</p></form></main>}
