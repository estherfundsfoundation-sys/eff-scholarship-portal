import {notFound} from "next/navigation";
import {CalendarDays,CheckCircle2,HeartHandshake} from "lucide-react";
import {createClient} from "@/lib/supabase/server";
import {startApplication} from "./actions";

export default async function ProgramDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const supabase=await createClient();const {data:program}=await supabase.from("programs").select("id,slug,name,description,program_cycles(id,name,opens_at,closes_at,status,award_config,form_versions(id,version,published_at))").eq("slug",slug).single();if(!program)notFound();const cycles=program.program_cycles as unknown as Array<{id:string;name:string;closes_at:string|null;status:string;award_config:Record<string,string>;form_versions:Array<{id:string;version:number;published_at:string|null}>}>;const cycle=cycles.find(item=>item.status==="open");if(slug!=="name-your-need")return <main className="section"><div className="shell" style={{maxWidth:850}}><div className="eyebrow">EFF-administered program</div><h2>{program.name}</h2><p className="muted">{program.description}</p><div className="notice"><strong>Applications are not currently open.</strong><br/>Program details will be published after EFF approves the cycle’s eligibility, award terms, and deadline.</div></div></main>;const form=cycle?.form_versions.find(item=>item.published_at);return <main><section className="program-hero"><div className="shell"><div className="eyebrow">Official 2026 application</div><h1>The “Name Your Need” Scholarship</h1><p className="scripture">“For such a time as this.” — Esther 4:14</p><p>You tell us, in your own words, what you need most to stay enrolled—and our team steps in to help.</p>{cycle&&form?<form action={startApplication}><input type="hidden" name="cycle_id" value={cycle.id}/><input type="hidden" name="form_version_id" value={form.id}/><button className="button tan" type="submit"><HeartHandshake size={19}/> Start or continue application</button></form>:<div className="notice">Applications are currently closed.</div>}</div></section><section className="section white"><div className="shell split" style={{alignItems:"start"}}><div><div className="eyebrow">About this scholarship</div><h2>Name what you need</h2><p>This need-based award is designed to help undergraduate students address verified unmet financial needs that could interrupt enrollment.</p><div className="deadline-card"><CalendarDays/><div><strong>Application deadline</strong><br/>July 31, 2026 at 11:59 p.m. Eastern</div></div><p className="muted">Early application is encouraged. Award amounts are not guaranteed and will be determined by EFF after review and verification.</p></div><div className="card"><h3>Eligibility — you must meet all</h3><ul className="check-list">{["U.S. citizen, U.S. national, or permanent resident","Completed the FAFSA","Demonstrated unmet financial need verified by your college or university","Undergraduate student who has not earned a bachelor’s degree","Enrolled at an accredited U.S. college or university","No minimum GPA; open to students in every field"].map(item=><li key={item}><CheckCircle2 size={19}/><span>{item}</span></li>)}</ul><p><strong>If you do not meet every requirement, please do not apply.</strong></p></div></div></section></main>}
