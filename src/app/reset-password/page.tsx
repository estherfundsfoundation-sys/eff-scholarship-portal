import {updatePassword} from "@/app/auth/actions";
export default async function Reset({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="section"><div className="shell" style={{maxWidth:520}}><div className="card"><div className="eyebrow">Choose a new password</div><h2>Secure your account</h2>{error&&<p className="notice" role="alert">{error}</p>}<form action={updatePassword} style={{display:"grid",gap:16}}><label>New password<input name="password" type="password" minLength={10} required autoComplete="new-password" style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><button className="button">Update password</button></form></div></div></main>}
