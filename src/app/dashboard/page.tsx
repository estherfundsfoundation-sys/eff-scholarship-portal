import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {signOut} from "@/app/auth/actions";import {applicantLabels,type ApplicationStatus} from "@/lib/domain";
export default async function Dashboard(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const [{data:profile},{data:applications},{data:requests}]=await Promise.all([supabase.from("profiles").select("legal_name,preferred_name,institution,degree_level,major,phone").eq("id",user!.id).single(),supabase.from("applications").select("id,status,updated_at,program_cycles(name,programs(name))").order("updated_at",{ascending:false}),supabase.from("information_requests").select("id,item,due_at,application_id").is("resolved_at",null)]);const name=profile?.preferred_name||profile?.legal_name||"there";const profileComplete=Boolean(profile?.legal_name&&profile?.institution&&profile?.degree_level);return <main className="section"><div className="shell"><div style={{display:"flex",justifyContent:"space-between",gap:20,alignItems:"start"}}><div><div className="eyebrow">Applicant portal</div><h2>Your dashboard</h2><p className="muted">Welcome, {name}. Here’s what needs your attention.</p></div><form action={signOut}><button className="button outline">Sign out</button></form></div><div className="split" style={{alignItems:"start",marginTop:30}}><section><h3>Applications</h3>{applications?.length?applications.map((a)=><article className="card" key={a.id} style={{marginTop:12}}><small className="eyebrow">{applicantLabels[a.status as ApplicationStatus]??a.status}</small><h3>{(a.program_cycles as unknown as {programs:{name:string}})?.programs?.name??"EFF application"}</h3><p className="muted">Updated {new Date(a.updated_at).toLocaleDateString()}</p><Link className="button" href={`/applications/${a.id}`}>View application</Link></article>):<div className="card" style={{marginTop:12}}><h3>No applications yet</h3><p className="muted">When an EFF cycle opens, you can start here and save your progress.</p><Link className="button" href="/programs">Explore EFF programs</Link></div>}</section><aside><div className="card"><h3>Profile</h3><p className="muted">{profileComplete?"Your reusable applicant profile is ready.":"Complete your reusable applicant profile before submitting."}</p><Link className="button outline" href="/profile">{profileComplete?"Review profile":"Complete profile"}</Link></div><div className="notice" style={{marginTop:16}}><strong>{requests?.length?`${requests.length} information request${requests.length===1?"":"s"}`:"No new requests"}</strong><br/>{requests?.length?requests[0].item:"Information requests and decisions will appear here and in your email."}</div></aside></div></div></main>}
