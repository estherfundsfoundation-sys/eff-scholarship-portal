import Link from "next/link";
import {createClient} from "@/lib/supabase/server";
import {activateApplyAll} from "../actions";

export default async function ApplyAllStart({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const db=await createClient();const {data:{user}}=await db.auth.getUser();return <main className="applyall"><section className="applyall-workspace"><div className="shell" style={{maxWidth:760}}><span className="applyall-kicker">EFF Apply Everywhere</span><h1>Start here.</h1><p className="applyall-lead">Create a separate Future Passport for your college journey. Your EFF scholarship applications remain separate and are never copied into university applications without your approval.</p>{error&&<p className="applyall-alert" role="alert">{error}</p>}<div className="card"><h2>Your secure starting point</h2>{user?<><p>You are signed in as <strong>{user.email}</strong>. Activate your dedicated Apply Everywhere profile to begin selecting schools.</p><form action={activateApplyAll}><button className="button">Activate my Apply Everywhere profile</button></form></>:<><p>Create or sign in to your secure EFF account. After verification, you will return here to activate Apply Everywhere.</p><div className="hero-actions"><Link className="button" href="/sign-up?next=%2Fapply-everywhere%2Fstart">Create my account</Link><Link className="button outline" href="/sign-in?next=%2Fapply-everywhere%2Fstart">Sign in</Link></div></>}</div><p className="applyall-fine">One secure login may access multiple EFF products, but Apply Everywhere maintains a separate profile, permissions, applications, and consent history.</p></div></section></main>}
