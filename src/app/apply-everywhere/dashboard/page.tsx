import Link from "next/link";import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {removeSchool} from "../actions";
type Selection={id:string;admission_term:string;applyall_institutions:{name:string;state_code:string;application_url:string|null}|null};
export default async function ApplyAllDashboard({searchParams}:{searchParams:Promise<{message?:string}>}){const {message}=await searchParams;const db=await createClient();const {data:{user}}=await db.auth.getUser();if(!user)redirect("/sign-in?next=%2Fapply-everywhere%2Fdashboard");const {data:profile}=await db.from("applyall_profiles").select("id").eq("user_id",user.id).maybeSingle();if(!profile)redirect("/apply-everywhere/start");const {data}=await db.from("applyall_school_selections").select("id,admission_term,applyall_institutions(name,state_code,application_url)").eq("profile_id",profile.id).order("selected_at");const selections=(data??[]) as unknown as Selection[];return <main className="applyall"><section className="applyall-workspace"><div className="shell"><span className="applyall-kicker">Your Future Passport</span><h1>Welcome to your journey.</h1><p className="applyall-lead">Complete one real EFF admissions application and authorize delivery to verified participating colleges.</p>{message&&<p className="applyall-complete"><strong>{message}</strong></p>}<section className="applyall-next"><div><span>Your Next Three Moves</span><h2>Build your application.</h2><ol><li>Complete your EFF common application</li><li>Select colleges</li><li>Review and authorize participating-college delivery</li></ol></div></section><div className="hero-actions"><Link className="button" href="/apply-everywhere/application">Complete my EFF application</Link><Link className="button outline" href="/apply-everywhere/schools">Select colleges</Link></div><h2 style={{marginTop:"2rem"}}>My college list</h2>{selections.length?<div className="applyall-review-list">{selections.map((selection)=><article className="card" key={selection.id}><strong>{selection.applyall_institutions?.name}</strong><p>{selection.applyall_institutions?.state_code} · {selection.admission_term}</p><small>EFF participation will be confirmed before submission.</small><div className="hero-actions">{selection.applyall_institutions?.application_url&&<a className="button outline" href={selection.applyall_institutions.application_url} target="_blank" rel="noopener noreferrer">Institution website</a>}<form action={removeSchool}><input type="hidden" name="selectionId" value={selection.id}/><button className="button outline">Remove</button></form></div></article>)}</div>:<div className="card"><p>No colleges selected yet.</p><Link className="button" href="/apply-everywhere/schools">Explore Southern colleges</Link></div>}</div></section></main>}
