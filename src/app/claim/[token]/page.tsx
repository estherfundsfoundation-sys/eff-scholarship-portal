import Link from "next/link";
import {createClient} from "@/lib/supabase/server";
import {claimLegacy} from "./actions";
export default async function Claim({params}:{params:Promise<{token:string}>}){const {token}=await params;const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();return <main className="section"><div className="shell" style={{maxWidth:720}}><div className="card"><div className="eyebrow">Secure application invitation</div><h2>Claim your existing application</h2><p>Esther Funds Foundation has moved your existing Name Your Need Scholarship application into this secure portal.</p><div className="notice"><strong>Your privacy matters.</strong><br/>We will verify that your signed-in email matches the invitation before connecting any application information.</div>{user?<form action={claimLegacy} style={{marginTop:24}}><input type="hidden" name="token" value={token}/><button className="button">Verify email and claim application</button></form>:<div className="hero-actions"><Link className="button" href={`/sign-up?claimToken=${encodeURIComponent(token)}&next=/claim/${encodeURIComponent(token)}`}>Create an account</Link><Link className="button outline" href={`/sign-in?next=/claim/${encodeURIComponent(token)}`}>Sign in</Link></div>}<p className="muted">Questions? Email <a className="card-link" href="mailto:nationals@estherfundsinc.org">nationals@estherfundsinc.org</a>.</p></div></div></main>}
