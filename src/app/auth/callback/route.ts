import { NextResponse } from "next/server";import { createClient } from "@/lib/supabase/server";import {safeInternalPath} from "@/lib/security";
export async function GET(request:Request){const url=new URL(request.url);const code=url.searchParams.get("code");const next=safeInternalPath(url.searchParams.get("next"));if(code){const supabase=await createClient();const {error}=await supabase.auth.exchangeCodeForSession(code);if(!error)return NextResponse.redirect(new URL(next,url.origin));}const retry=next==="/reset-password"?"/forgot-password?error=That+reset+link+is+invalid+or+expired.+Request+a+new+link+and+use+only+the+newest+email.":"/sign-in?error=That+link+is+invalid+or+expired.";return NextResponse.redirect(new URL(retry,url.origin));}
