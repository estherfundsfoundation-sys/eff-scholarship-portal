import Link from "next/link";import {signIn} from "@/app/auth/actions";
export default async function SignIn({searchParams}:{searchParams:Promise<{error?:string;message?:string;next?:string}>}){const p=await searchParams;return <main className="section"><div className="shell" style={{maxWidth:520}}><div className="card"><div className="eyebrow">Welcome back</div><h2>Sign in</h2>{p.error&&<p className="notice" role="alert">{p.error}</p>}{p.message&&<p className="notice" role="status">{p.message}</p>}<form action={signIn} style={{display:"grid",gap:16,marginTop:24}}><input type="hidden" name="next" value={p.next??"/dashboard"}/><label>Email address<input name="email" type="email" autoComplete="email" required style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><label>Password<input name="password" type="password" autoComplete="current-password" required style={{display:"block",width:"100%",padding:12,marginTop:6}}/></label><button className="button">Sign in securely</button></form><p><Link className="card-link" href="/forgot-password">Forgot your password?</Link></p><p className="muted">New to the portal? <Link className="card-link" href="/sign-up">Create an account</Link></p></div></div></main>}
