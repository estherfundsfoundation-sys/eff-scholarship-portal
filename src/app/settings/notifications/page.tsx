import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {savePreferences} from "./actions";
export default async function NotificationSettings(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const {data:prefs}=await supabase.from("notification_preferences").select("application_email,scholarship_reminders,administrative_notices").eq("user_id",user!.id).maybeSingle();return <main className="section"><div className="shell" style={{maxWidth:760}}><Link className="card-link" href="/dashboard">← Dashboard</Link><div className="eyebrow">Your communication choices</div><h2>Notification preferences</h2><form action={savePreferences} className="card stack"><label><input type="checkbox" checked readOnly/> Essential application updates (required while an application is active)</label><label><input type="checkbox" name="scholarship_reminders" defaultChecked={prefs?.scholarship_reminders??false}/> Deadline reminders for scholarships you choose</label><label><input type="checkbox" name="administrative_notices" defaultChecked={prefs?.administrative_notices??true}/> Important EFF administrative notices</label><button className="button">Save preferences</button></form><p className="muted">Delivery suppressions and provider bounces may override these settings. Contact nationals@estherfundsinc.org for help.</p></div></main>}
