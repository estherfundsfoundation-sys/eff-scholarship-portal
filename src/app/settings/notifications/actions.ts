"use server";import {revalidatePath} from "next/cache";import {createClient} from "@/lib/supabase/server";
export async function savePreferences(formData:FormData){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Please sign in again.");const {error}=await supabase.from("notification_preferences").upsert({user_id:user.id,application_email:true,scholarship_reminders:formData.get("scholarship_reminders")==="on",administrative_notices:formData.get("administrative_notices")==="on",updated_at:new Date().toISOString()},{onConflict:"user_id"});if(error)throw new Error("Preferences could not be saved.");revalidatePath("/settings/notifications")}
