import {describe,expect,it} from "vitest";
import {formatSupportAmount,normalizeSupportUrl,studentInitials,studentProfileSlug,submittedStudentSupportSchema} from "./student-support";
describe("student support directory safeguards",()=>{
  it("accepts public HTTPS fundraising links",()=>{expect(normalizeSupportUrl("https://www.gofundme.com/f/help-a-student#story")).toEqual({url:"https://www.gofundme.com/f/help-a-student",platform:"GoFundMe"});expect(normalizeSupportUrl("https://givebutter.com/student-help")?.platform).toBe("Givebutter");});
  it("rejects unsafe destinations",()=>{expect(normalizeSupportUrl("http://gofundme.com/f/example")).toBeNull();expect(normalizeSupportUrl("https://user:pass@example.com/help")).toBeNull();expect(normalizeSupportUrl("https://localhost/help")).toBeNull();expect(normalizeSupportUrl("https://192.168.1.25/help")).toBeNull();});
  it("creates stable display helpers",()=>{expect(studentProfileSlug("Jos\u00e9 Student","12345678-aaaa-bbbb-cccc-123456789000")).toBe("jose-student-12345678");expect(studentInitials("Taylor Morgan")).toBe("TM");expect(formatSupportAmount(2500)).toBe("$2,500");});
  it("requires consent before submission",()=>{const base={displayName:"Taylor M.",headline:"Help me stay enrolled this semester",institution:"Example University",locationText:"Atlanta, GA",major:"Biology",classYear:"Junior",story:"A".repeat(150),needSummary:"B".repeat(40),amountNeeded:"2500",fundraiserUrl:"https://gofundme.com/f/example",consentConfirmed:true as const,accuracyConfirmed:true as const,ageConfirmed:true as const,safetyConfirmed:true as const};expect(submittedStudentSupportSchema.safeParse(base).success).toBe(true);expect(submittedStudentSupportSchema.safeParse({...base,consentConfirmed:false}).success).toBe(false);});
});
